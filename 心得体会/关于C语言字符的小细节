1.char的类型别存字符串（作者说空间比较小，只有一字节），用int类型来从缓冲区读取空间比较大（有四字节）
2.单个字符一定要用‘’，别用到“”了，用错了编译器不报错的！
3.当while（c=getchar（））！=EOF，在windows中用Alt+z，然后回车才能结束（别回车半天发现结束不了）
------------
作业1-9
搞了个count来记录空格的个数，当超过两个就给c返回0，碰到非0就让记录清空（哇，想出这个是真的花了点时间，记录一下思路）
作业1-10
服了这回退，检测有点麻烦，就不检测了吧（\t都成功了，\b没有不成功的道理😡）。
printf打印\\要写\\\\，在这里栽跟头了。以后注意点
